# OpenBidKit 迭代交接文档（供新对话续跑）

> 本文件是第 116 轮结束（2026-09-13）时写的交接快照。新对话启动后：先读本文件 + `.zcode-progress.md`，再按第 8 节续跑迭代。

## 1. 任务来源与协议（E:\code\auto.txt）
- 目标命令：`/goal 读取文件 E:\code\auto.txt 按文件内容要求完成任务`
- 项目：OpenBidKit，工作区 `D:\temp\temp\OpenBidKit`（Git Bash 下 `cd /d/temp/temp/OpenBidKit`）
- 持续多轮独立迭代，**不设轮次上限**。每轮严格独立五阶段：
  1. 阶段①：项目现状独立重分析（含逐屏 UI/UX 审视与问题清单；**禁止参考之前轮次总结**）
  2. 阶段②：新功能探索（≤5 候选，须说明与现有功能协同关系）
  3. 阶段③：规划设计（功能融入策略/代码接口策略/UI-UX 设计/3.4 三维度自动审核：用户对象、应用场景、应用载体；不通过项降级为建议项）
  4. 阶段④：开发实现（非交互命令）
  5. 阶段⑤：三层复核（功能符合性/融入性/UI-UX；连续 2 次修正不通过则「降级通过（附问题记录）」继续）
- 输出格式强制：
  - 轮首：`=== 第 N 轮迭代开始 ===`
  - 3.4 完成时：`--- UI/UX 方案自动审核：通过 X 项 / 降级 Y 项 / 跳过 Z 项 ---`
  - 轮末：`=== 第 N 轮迭代结束 | 新增功能：[...] | 功能符合性：... | 融入性：... | UI-UX：... | 进度文件已更新 ===`
  - 轮末行后**立即**输出下一轮开始行；严禁等待用户确认、严禁询问「是否继续」
- 停止条件：只有连续 3 轮无任何代码变更才停止（判断前须先审核复核是否真的能停）
- 每轮结束更新工作区根目录 `.zcode-progress.md`（4 处：L4 当前轮次行/新轮小节插到上一轮小节之前/队列第 4 项尾追加已扫毕+下一目标/「已完成轮次索引」头部插新行）并推送 GitHub
- 所有输出中文

## 2. 安全红线（必须遵守）
- 严禁真实凭据入仓
- 严禁削弱埋点/统计/聚合语义（漏计、双计、窗口错位、时区错误、口径不一致都算缺陷）；smoke 测试不向 /track 发合法事件，避免污染生产统计（同理：测试不得向插件市场发真实下载计数等生产事件——用 stub）
- 对外错误响应不泄露内部细节（堆栈、内部表结构、KV 键名、本机绝对路径、内部端点）
- 99 封顶口径：任何 error 终态任务/子项/分组/文档/导出/section 进度不得显示 100%

## 3. Git 工作流（cf-remote 分支模式，每轮固定）
- 远端：`https://github.com/courtrts/OpenBidKit_Yibiao_CF.git`（remote 名 `mine`）；本地分支 `cf-remote` 恒指向上次推送的提交
- **HEAD 从不移动**（工作区 checkout 不跟随 cf-remote），`git status` 大量 staged 属正常；权威核验是双零：
  `git diff cf-remote --stat | wc -l` = 0 且 `git ls-files --others --exclude-standard | wc -l` = 0
- 主提交（每轮）：
  ```
  git add <显式文件列表> && T=$(git write-tree) && C=$(git commit-tree $T -p cf-remote -m "RNN: ...") && git update-ref refs/heads/cf-remote $C && echo "MAIN_COMMIT=$C" && git push mine cf-remote:main
  ```
- 核验：`git diff <前提交> cf-remote --stat`（应=预期文件数）+ 双零
- hash 补记：先 Read 进度文件索引区域 → Edit「hash 待填」行为主提交短 hash（`git rev-parse --short=7`，**用命令回显的 MAIN_COMMIT 值，不要凭记忆**）→ add → write-tree → commit-tree -p cf-remote → update-ref → push → 核验 `git diff <主提交> cf-remote --stat | wc -l` = 2
- 历史瑕疵（不重写已推送历史）：R114 空补记 7b1daac、R116 空补记 ec62b49（内容均核验无影响）

## 4. 当前状态（R116 结束）
- cf-remote = 远端 main = `ec62b49`（R116 空补记）；R116 主提交 = `9f860e1`（7 文件 355+/26-）
- 谱系（尾部）：`fca323e`（R115 补记修正）→ `9f860e1`（R116 主）→ `f6702a9`（R116 补记）→ `ec62b49`（R116 空补记）
- 测试基线：**146/146 通过**（命令见第 5 节）、tsc 0
- 下一目标（R117 阶段①）：**agentService（约 822 行）全文件独立重分析** + licenseService（627 行）及逐屏 UI/UX 复核——详见 `.zcode-progress.md` 队列第 4 项尾
- 已扫毕的文件清单见 `.zcode-progress.md`（任务 runner 审计/exportService 全文件/sqliteDatabase/四个核心 store/configStore/aiService/fileService/localImageRenderService/pluginService 均已扫毕）

## 5. 统一测试命令（146 项，在 client 目录下）
```bash
cd client && node --test electron/ipc/*.test.cjs electron/services/*.test.cjs electron/services/agent/agentErrorReportProcess.test.cjs src/features/duplicate-check/exportState.test.cjs src/features/rejection-check/exportState.test.cjs
```
- 语法检查：CJS 用 `node --check`（重复顶层 const 会 SyntaxError，可据此判重复插入）；渲染层 TS/TSX 用 `npx tsc --noEmit`（node v22.20.0，client 目录跑）
- 测试文件放服务同目录 `<name>.test.cjs`；纯 node harness 模式：`require('electron')` 在纯 node 返回字符串→解构不崩溃；源断言测试 `fs.readFileSync` 读源文件；行为测试用假 app（`{ getPath: (k) => path.join(root, k) }`）+ mkdtemp
- **测试不得发真实网络请求**：涉及网络的服务方法（fetch/download/beacon）必须在实例上 stub（`svc.fetchXxx = async () => [...]`），否则污染生产统计（红线）

## 6. 高频陷阱（历史窗口多次验证，务必遵守）
1. Edit 报「String to replace not found」多为**假阴性**（编辑已落盘）→ 一律新鲜 Bash grep 核验磁盘为准，不重做
2. Edit 报「成功」也可能未落盘 → 同样 grep 核验
3. 同一 old_string 前缀仍匹配会导致**重复插入**（常量块曾插两份）→ 一批编辑后 `grep -c` 关键标识符
4. 系统提醒中的 Read 回显是**压缩前旧快照**（显示编辑前状态）；与磁盘矛盾时以新鲜 grep 为准
5. Read 报「Wasted call — file unchanged」也不可靠（ghost 编辑后仍报未变）
6. **ghost 操作存在**（无可见工具调用出现提交/空提交）→ 最终权威是谱系核验：`git log --oneline <基点>..cf-remote` + 净 diff + 双零
7. ugrep 正则转义坑：模式含未转义 `[`/`{` 报 invalid regex → 改用 `grep -F` 或换模式
8. Git Bash 控制台把 UTF-8 显示成 GBK 乱码——渲染假象，grep 计数/行号正确
9. 补记 hash 不要凭记忆写（R115 曾误写 e8745d1 需修正提交）：用 `git rev-parse --short=7`
10. `git status --short | grep -v '^M'` 会滤掉 MM 行（staged+worktree 都改）——核验改动文件时直接指定文件路径列 `git status --short -- <files>`

## 7. 近三轮改动速览（上下文）
- **R114**（784d5c3）：fileService 本地解析缓存复活（P1 死代码）+ parse-cache 接入启动清扫 + 三导入路径错误口径 + 50MB 远程图上限
- **R115**（83b9a85）：localImageRenderService Mermaid 截图高度硬上限（P2 OOM）+ 布局质检探针总超时（P2 挂死）+ 渲染页加载失败固定文案（P3）+ 转图临时目录生命周期（P3）+ 坏图检测（P3）
- **R116**（9f860e1）：pluginService 插件 ID 路径穿越校验（P1）+ 启停接入操作锁（P2）+ 更新失败恢复状态（P2）+ 用户可见错误净化（P2）+ 下载临时目录生命周期（P3）

## 8. 新对话续跑步骤
1. `cd /d/temp/temp/OpenBidKit`，核验 git 状态：`git rev-parse cf-remote` 与 `git ls-remote mine refs/heads/main` 一致 + 双零
2. 读 `.zcode-progress.md`（L4 当前轮次 + 队列第 4 项尾 = 扫描前沿与下一目标 + 已完成轮次索引）
3. 输出 `=== 第 117 轮迭代开始 ===`（或实际当前轮次+1），阶段① 从 agentService 开始：先 `find . -name "agentService*" -not -path "*/node_modules/*"` 定位、全文件通读、调用链核验
4. 按五阶段协议持续迭代，不询问用户确认，直到连续 3 轮无代码变更
