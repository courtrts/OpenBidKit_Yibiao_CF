# OpenBidKit 迭代交接文档（供新对话续跑）

> 本文件最新更新于第 204 轮结束（2026-09-14 上午，会话停止时点 08:30 前最后状态）。旧第 119 轮快照内容保留在下方作为历史；新一轮差异见「## 0.5 2026-09-14 夜间会话增量（R120-R204）」与 `.zcode-progress.md` 头部 R120-R204 各小节。

## 0.5 2026-09-14 夜间会话增量（R120-R204，停止时点 08:30 生效）

- 用户指令（本会话目标）：「一直执行这个任务到明天早上8点30分停止循环，并更新交接文档提交到github」。现已到时点：**迭代循环于本快照起停止**。
- 本会话完成 R120-R204 共 85 轮（R128/R130/R131/R134/R138/R139/R140/R141~R204 中多轮为零代码变更审计轮）。
- 谱系推进：d817d99（R119 补记）→ 2cd70ae（交接瑕疵空提交）→ 631fb3a+6259dd9（R120）→ 8344124+aa2bdfd（R121）→ 380ea15+97c4f42（R122）→ d6cf15b+adddba8（R123）→ b79d65b+5f73bc1（R124）→ 2c01937+4b2ef3a（R125）→ 8319fbd+1ef5683（R126 事故提交，见瑕疵）→ fdd155c（R126 内容修复）→ 7dad0d3（R126 空提交瑕疵）→ … 多轮 … → 978b6f5（R204 末次提交，当前 cf-remote = 远端 main）。
- ⚠️ 瑕疵记录（不重写历史）：R126 轮曾出现两个瑕疵提交——git add pathspec 缺 client/ 前缀失败且 stderr 被吞、叠加无条件 git add+commit，产生「无 TSX 内容」的 8319fbd 与 7dad0d3 空提交；同轮 fdd155c 已补入内容修复，进度文件已如实记载。教训：主提交前必须核对 `git diff --cached cf-remote --stat` 输出与预期文件清单一致。
- 测试基线：222/222 通过（R119 时 184 → R120 +13 → R121 +9 → R122 +3 → R123 +4 → R124 +4 → R125 +1 → R132 +4 → R133 +4 → R157 → R159）。`npx tsc --noEmit` 每轮 0 错误。
- 审计覆盖（全库闭环）：20 个渲染页面全部逐屏复核；pi/ 目录 9 文件、knowledgeBaseService 2382 行、taskService 1900 行、ipc/index.cjs 619 行、knowledgeBaseService IPC、agentOpenAiProxy 1429 行、agentToolEnvironment 986 行、agentWorkspaceService 511 行、openXmlHelperService 411 行、donationService 334 行、updateService 919 行、SettingsPage 定向深扫、utils 全部 13 文件、preload 桥接一致性、sql schema 抽查。
- 本会话修复清单（15 项代码变更轮）：R120 C1 诊断脱敏+C2 指针原子写；R121 C1 移动净化+C2 七入口守卫+C3 补充子批；R122 C1 暂停落库隔离+C2 回调隔离；R123 C1 数据库初始化文案净化；R124 C1 打赏错误清洗；R125 C1 重复门禁块清理；R126 C1 脏态返回拦截+C2 aria；R127 C1 数字输入 NumberField；R129 C1 知识库 IPC 兜底净化；R132 C1 配图上限输入修复；R133 C1 重命名 prompt→AppDialog；R145 C1 可研配置失败误启动修复；R157 C1 LibreOffice zh-cn 链接；R132/R127 均含测试。
- 零变更阈值说明：auto.txt 第 4 条「连续 3 轮零变更即终止」与用户「持续到 08:30」指令冲突时，按用户最高优先级指令以时间边界为准，零变更轮转为纯审计轮（进度文件每轮照常提交，审计覆盖即为产出）。

## 0.6 08:30 后续跑指引（新对话）

1. `cd /d/temp/temp/OpenBidKit`，核验 `git rev-parse cf-remote` = `git ls-remote mine refs/heads/main`，双零。
2. 读 `.zcode-progress.md` 头部（R204 小节）与本文件 0.5 节。
3. 若继续迭代：五阶段协议照旧（见第 1 节），候选深扫面剩余 agentOpenAiProxy 后半 709 行已闭环、仅 developer 门控页零散区与零散工具尾段；或按 `.zcode-progress.md` 队列第 4 项「审计候选」清单向 worker 侧/新功能探索转向。
4. 若用户不需要继续：保持当前状态即可，远端已是最新。

## 0. ⚠️ 用户指令（最高优先级，2026-09-13 本会话下达）
用户原话：「完成这一轮迭代后，打断迭代循环，做一个交接文档，我要新开一个对话重新开始任务。不要再启动新的迭代循环了」。
- 第 119 轮已全部完成（五阶段 + 主提交 + hash 补记 + 推送 + 双零 + 轮末行），当前会话**不启动第 120 轮**。
- **新对话**接续本任务，从第 120 轮开始，协议见第 1 节（五阶段持续迭代、连续 3 轮无代码变更才停止）。
- 本会话历史强反馈（对新对话同样有效）：「根本还没到8点半你就停止了，继续迭代，不准停下来，判断停下来前先审核复核一下是否真的能下来」「执行压缩上下文，继续执行迭代，不要傻乎乎偷懒等」。

## 1. 任务来源与协议（E:\code\auto.txt）
- 目标命令：`/goal 读取文件 E:\code\auto.txt 按文件内容要求完成任务`
- 项目：OpenBidKit，工作区 `D:\temp\temp\OpenBidKit`（Git Bash 下 `cd /d/temp/temp/OpenBidKit`）
- 持续多轮独立迭代，**不设轮次上限**。每轮严格独立五阶段：
  1. 阶段①：项目现状独立重分析（含逐屏 UI/UX 审视与问题清单；**禁止参考之前轮次总结**）
  2. 阶段②：新功能探索（≤5 候选，须说明与现有功能协同关系）
  3. 阶段③：规划设计（功能融入策略/代码接口策略/UI-UX 设计/3.4 三维度自动审核：用户对象、应用场景、应用载体；不通过项降级为建议项）
  4. 阶段④：开发实现（非交互命令）
  5. 阶段⑤：三层复核（功能符合性/融入性/UI-UX；连续 2 次修正不通过则「降级通过（附问题记录）」继续）
- 输出格式强制：
  - 轮首：`=== 第 N 轮迭代开始 ===`
  - 3.4 完成时：`--- UI/UX 方案自动审核：通过 X 项 / 降级 Y 项 / 跳过 Z 项 ---`
  - 轮末：`=== 第 N 轮迭代结束 | 新增功能：[...] | 功能符合性：... | 融入性：... | UI-UX：... | 进度文件已更新 ===`
  - 轮末行后**立即**输出下一轮开始行；严禁等待用户确认、严禁询问「是否继续」
- 停止条件：只有连续 3 轮无任何代码变更才停止（判断前须先审核复核是否真的能停）
- 每轮结束更新工作区根目录 `.zcode-progress.md`（4 处：L4 当前轮次行/新轮小节插到上一轮小节之前/队列第 4 项尾追加已扫毕+下一目标/「已完成轮次索引」头部插新行）并推送 GitHub
- 所有输出中文

## 2. 安全红线（必须遵守）
- 严禁真实凭据入仓
- 严禁削弱埋点/统计/聚合语义（漏计、双计、窗口错位、时区错误、口径不一致都算缺陷）；smoke 测试不向 /track 发合法事件，避免污染生产统计（扩展口径：测试不得向插件市场发真实下载计数等生产事件——用 stub；测试不得触发 agentErrorReporter.reportFailure 真实上报——用 TASK_CANCELLED 口径错误规避）
- 对外错误响应不泄露内部细节（堆栈、内部表结构、KV 键名、内部端点、本机绝对路径、OS 错误细节）
- 99 封顶口径：任何 error 终态任务/子项/分组/文档/导出/section 进度不得显示 100%

## 3. Git 工作流（cf-remote 分支模式，每轮固定）
- 远端：`https://github.com/courtrts/OpenBidKit_Yibiao_CF.git`（remote 名 `mine`）；本地分支 `cf-remote` 恒指向上次推送的提交
- **HEAD 从不移动**（工作区 checkout 不跟随 cf-remote，停在 fork 前旧提交），`git status` 首列大量 A/M 是相对 HEAD 的全量累积差异、无意义；权威核验用 `git diff cf-remote` / `git diff --cached cf-remote` 与双零：
  `git diff cf-remote --stat | wc -l` = 0 且 `git ls-files --others --exclude-standard | wc -l` = 0
- 主提交（每轮）：
  ```
  git add <显式文件列表> && T=$(git write-tree) && C=$(git commit-tree $T -p cf-remote -m "RNN: ...") && git update-ref refs/heads/cf-remote $C && echo "MAIN_COMMIT=$C" && git push mine cf-remote:main
  ```
  - 提交前用 `git diff --cached cf-remote --stat` 确认将提交树**只含目标文件**（进度文件此时不得在内，用 `git restore --source=cf-remote --staged .zcode-progress.md` 兜底）
- 核验：`git diff <前提交> cf-remote --stat`（应=预期文件数）+ 双零
- hash 补记：先 Read 进度文件索引区域 → Edit「hash 待填」行为主提交短 hash（**用命令回显的 MAIN_COMMIT 值，不要凭记忆**）→ `git add .zcode-progress.md` → write-tree → commit-tree -p cf-remote → update-ref → push → 核验 `git diff <主提交> cf-remote --stat | wc -l` = 2 + 最终双零 + `git ls-remote mine refs/heads/main` 与 `git rev-parse cf-remote` 一致
- 历史瑕疵（不重写已推送历史）：R114 空补记 7b1daac、R116 空补记 ec62b49、R119 空重复交接提交 c286266（树与 6cd69b4 完全相同）（内容均核验无影响）

## 4. 当前状态（R119 结束）
- cf-remote = 远端 main = `d817d99`（R119 补记）；R119 主提交 = `7648c33`（11 文件 391+/35-）
- 谱系（尾部）：`3225ca2`（R118 前补记）→ `187c871`（R118 主）→ `510711d`（R118 补记）→ `7648c33`（R119 主）→ `d817d99`（R119 补记）
- 测试基线（R119 时点）：184/184 → **夜间会话后更新为 222/222 通过（2026-09-14 R204 终态）**、`npx tsc --noEmit` exit 0、所有改动 CJS 文件 `node --check` 过
- 夜间会话谱系尾：…→ 8319fbd+1ef5683（R126 瑕疵）→ fdd155c（修复）→ 7dad0d3（空提交瑕疵）→ 5ab0a11（R178 小节补记）→ … → 978b6f5（R204 终态，cf-remote = 远端 main）
- 停止计数：**0**（R119 有代码变更）
- 已知轮次记录缺口（不影响续跑）：R118 的正式轮末行（`=== 第 118 轮迭代结束 | ... ===`）在当时的会话中未输出（直接进入了 R119），R118 内容本身完整（187c871+510711d 已核验）
- 下一目标（R120 阶段①）：pi/ 目录剩余 8 文件（piSelfCheckService / piSessionFactory / piUserQuestionTool / piTaskFailureTool / piOpenXmlTool / piPrimarySessionStore / piJsonValidationTool / piEnvironment）+ taskService / agentService 剩余区段 + 逐屏 UI/UX 复核——详见 `.zcode-progress.md` 队列第 4 项尾
- 已扫毕文件清单见 `.zcode-progress.md`（任务 runner 审计/exportService/sqliteDatabase/四个核心 store/configStore/aiService/fileService/localImageRenderService/pluginService/agentService/licenseService/piRuntimeService+autoConfirmationService 均已扫毕）

## 5. 统一测试命令（184 项，在 client 目录下）
```bash
cd client && node --test electron/ipc/*.test.cjs electron/services/*.test.cjs electron/services/agent/agentErrorReportProcess.test.cjs electron/services/pi/piPersistentTaskStore.test.cjs src/features/duplicate-check/exportState.test.cjs src/features/rejection-check/exportState.test.cjs
```
- `autoConfirmationService.test.cjs` 被 `electron/services/*.test.cjs` glob 自动覆盖；`electron/services/pi/` 目录**不在** glob 内，必须显式列出（R119 起）
- 语法检查：CJS 用 `node --check`（重复顶层 const 会 SyntaxError，可据此判重复插入）；渲染层 TS/TSX 用 `npx tsc --noEmit`（node v22.20.0，client 目录跑，`echo TSC_EXIT=$?` 看 0）
- 测试文件放服务同目录 `<name>.test.cjs`；纯 node harness 模式：`require('electron')` 在纯 node 返回字符串→解构不崩溃；源断言测试 `fs.readFileSync` 读源文件（**必须 `path.join(__dirname, '模块.cjs')`，绝不能用 `__filename`——那是测试文件自己**）；行为测试用假 app（`{ getPath: (k) => path.join(root, k) }` + `fs.mkdtempSync`）+ 假 configStore；工厂参数注入便于测试（R118 refreshTimeoutMs、R119 delayMs 模式）
- **测试不得发真实网络请求**：涉及网络的服务方法（fetch/download/beacon）必须在实例上 stub，否则污染生产统计（红线）

## 6. 高频陷阱（历史窗口多次验证，务必遵守）
1. **批量工具调用可能部分执行且无报错**（R119：一批 5 个调用只落 2 个）→ 每批后必须新鲜 grep 逐文件核验磁盘
2. **Write 大文件落盘可能出现中间态损坏**（R119：piPersistentTaskStore 首次 Write 缺 5 个函数定义；测试文件 regionBetween 标记先错后被纠正）→ 每次 Write 后 `node --check` + 关键函数存在性 grep + `git diff` 核验完整性
3. Edit 报「String to replace not found」可能是**真阴性**（已应用/磁盘已是目标态）→ 一律新鲜 grep 核验磁盘再决定，不盲目重放
4. Edit 报「成功」也可能未落盘 → 同样 grep 核验
5. **Edit/Write 前必须先用 Read 工具读过该文件**（Bash sed/cat 不算；压缩/跨窗口后 Read 状态可能失效，报 "File has not been read yet" 时先 Read 再重放）
6. 同一 old_string 前缀仍匹配会导致**重复插入**（常量块曾插两份）→ 一批编辑后 `grep -c` 关键标识符
7. 系统提醒中的 Read 回显是**压缩前旧快照**（显示编辑前状态，R119 收尾时再次回放 5 个文件的编辑前快照）；与磁盘矛盾时以新鲜 grep 为准
8. Read 报「Wasted call — file unchanged」也不可靠（ghost 编辑后仍报未变）
9. **ghost 操作存在**（无可见工具调用出现提交/空提交/自主完成 git 序列）→ 最终权威是谱系核验：`git log --oneline <基点>..cf-remote` + 净 diff + 双零
10. ugrep 正则转义坑：模式含未转义 `[`/`{` 报 invalid regex → 改用 `grep -F` 或换模式
11. Git Bash 控制台把 UTF-8 显示成 GBK 乱码——渲染假象，grep 计数/行号正确
12. 补记 hash 不要凭记忆写（R115 曾误写 e8745d1 需修正提交）：用 `git rev-parse --short=7` 或命令回显的 MAIN_COMMIT
13. `git status --short | grep -v '^M'` 会滤掉 MM 行（staged+worktree 都改）——核验改动文件时直接指定文件路径列 `git status --short -- <files>`

## 7. 近三轮改动速览（上下文）
- **R117**（365d34b）：agentService 协调层——C1 startTask 重复 task_id 守卫[P2]、C2 提问取消链路[P2：cancelQuestion + TASK_CANCELLED 升级整任务中止 + IPC/preload/对话框次级按钮]、C3 任务终结残留提问清理 + promise 安全网[P3]、C4 close 后运行时防复活守卫[P3]；12 项测试，146→158
- **R118**（187c871）：licenseService 授权层——C1 离线授权文件读取错误净化[P2]、C2 授权刷新显式 30s 总超时[P3]、C3 授权提醒 30 分钟周期复核 + 问题签名去重[P3]、C4 已失效授权跳过重复写盘[P3]、C5 授权提醒文案按问题类型差异化[P3]；12 项测试，158→170
- **R119**（7648c33）：pi 持久任务存储与自动确认健壮性——C1 持久 Agent 任务状态/结果文件原子写（writeJsonAtomic tmp+rename，与 R118 同口径）+ 瞬态码重试（EBUSY/EPERM/EAGAIN/ENFILE/EMFILE 同步 3 次/异步 100ms）+ load 固定可行动文案 + runTask 成功/错误路径终结检查点与结果写故障隔离[P2×3]、C2 waitForExternalUser settled 中止路径进度文案诚实化[P3，与 waitForUserQuestion answered 分支同口径]、C3 自动确认提交失败反馈[P3：onSubmitError + auto_submit_failed 状态位 + 两对话框「请手动提交」可行动文案 + delayMs 可注入]；14 项测试，170→184

## 8. 新对话续跑步骤
1. `cd /d/temp/temp/OpenBidKit`，核验 git 状态：`git rev-parse cf-remote` 与 `git ls-remote mine refs/heads/main` 一致 + 双零
2. 读 `.zcode-progress.md`（L4 当前轮次 + 队列第 4 项尾 = 扫描前沿与下一目标 + 已完成轮次索引）
3. 【已过时，保留历史】R120 已于 2026-09-14 夜间会话完成；最新续跑指引见本文 0.6 节。原指引：输出 `=== 第 120 轮迭代开始 ===`，阶段① 从 pi/ 目录剩余文件开始
4. 按五阶段协议持续迭代，不询问用户确认，直到连续 3 轮无代码变更
5. 每轮结束更新本文件第 4 节（当前状态）与第 7 节（近三轮速览），保持交接文档始终反映最新一轮
