# 迭代进度文件

## 当前轮次
第 44 轮待启动（第 43 轮已完成，远端 main = eb5831a，worker 部署 0f6f3ba7）

## 第 43 轮已完成（2026-09-12）
新增用户功能：
- 取消任务（重置/导入招标文件/清空下游）结算等待加 15s 兜底超时：runner 挂起时 IPC 不再永久转圈，操作必然返回（taskService.cjs waitForSettlementWithTimeout）
- 封禁 IP 退出由 process.exit(0) 改为 app.quit()：走 before-quit 清理流程，运行中任务 checkpoint/SQLite 落盘，GPU 探测定时器被清理（防下次启动误禁硬件加速）

一致性/修复（消化 R42 审计排队项 4/4 + 新审计 top3）：
- notice 删除：先删 D1 行再依最新 current 决定清 KV，消除基于过期快照误删新公告 KV / KV 先删 D1 失败的分叉窗口
- notice 保存补偿：旧行快照前置到 saveStoredNotice 之前，KV 写失败时 restoreStoredNotice（新 store 函数）把旧 client_notice_id 与送达计数原样恢复，送达上报不再 404
- license 双通道统一 resolveLicenseConfigOrDefaults：KV 故障时回落与 readLicenseConfig 相同的归一默认值，offline 通道不再采信请求体布尔值
- pluginStore：asset.size 无效时按 Release 未就绪处理（502+releaseNotReady）；isCompletePluginPackage 期望大小缺失一律判失败，坏包不再可能以 immutable 语义发布
- mermaidCache：rename 失败清 tmp + 重试一次，Windows 文件占用场景不再永久累积 .tmp

复核：node --check 全过、23/23 tests、tsc 0 错；worker 部署 0f6f3ba7，线上 health/notice/plugins 200。
降级/跳过：无。

## 待办项（审计排队，按序消化）
1. [P2] taskService：runner 启动前抛错（updateWorkspaceStateWithoutReload/emit 订阅者磁盘写）导致 activeTasks/activeTaskControls 永久泄漏、任务组锁死（初始化段包 try/catch 补两 Map 清理）
2. [P3] pluginConfigWindow loadFile 失败未捕获 → unhandledRejection+白屏（.catch 记录并关窗，或 did-fail-load 处理）
3. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId）
4. [P3] pluginStore sync skippedCount 新字段可在管理端核对（无阻塞）

## 已完成轮次索引
- 第 43 轮（eb5831a）：notice 双写一致性+license 统一兜底+asset.size 强校验+app.quit 落盘+结算超时+mermaid tmp 清理；worker 部署 0f6f3ba7
- 第 42 轮（564e54f）：可研删除/移除双重确认+保存禁用态+文案统一；时长抽共享；worker DELETE RETURNING 对账+sync mustExist
- 第 41 轮（de43f0d + 3f7d330）：AI 队列暂停拒绝统一落 paused 终态；HANDOFF 交接文档；notice 布尔归一（35b1a63）
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口；worker 部署 a21485de
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）
