# 迭代进度文件

## 当前轮次
第 45 轮待启动（第 44 轮已完成，远端 main = e21343b，dashboard 部署 617a3df3）

## 第 44 轮已完成（2026-09-12）
新增用户功能：
- 仪表盘切换项目后自动强制刷新全部表格并重置分页：消除「看着 A 项目列表删 B 项目公告/封 B 项目 IP」的误操作诱导
- 插件配置窗口加载失败时显示中文错误页并自动关窗，不再白屏 + unhandledRejection

一致性/修复：
- [P1 XSS] 仪表盘仓库链接仅放行 http(s)（render.js 新增 safeHttpUrl），plugins 表格不安全 repository 降级为「-」，overview GitHub 链接同口径；外链补 rel="noopener noreferrer"
- [P1 竞态] 仪表盘 refreshActiveTab 串行化（refreshChain 队列）：快速切 tab/改筛选/翻页时慢响应不再覆盖新参数的数据
- taskService 任务初始化段（磁盘写/emit 订阅者回调）包 try/catch：失败时与 runner 兜底同路径清理 activeTasks/activeTaskControls 并落 error 终态，不再残留僵尸任务锁死整组任务

复核：node --check（ESM+CJS）全过、23/23 tests；dashboard 部署 617a3df3，admin 域 200 且已在提供 safeHttpUrl 新代码，API health 200。
降级/跳过：无。

## 待办项（审计排队，按序消化）
1. [P2] 仪表盘加载失败时表格不清空、无过期标记（叠加 60s tab 缓存，管理员可能把陈旧数据当最新）——失败时渲染错误占位或加载前置 loading 态
2. [P2] 仪表盘 modelInfoCache/clients 页内部仍有 fire-and-forget 局部请求（loadModelInfoCache 搜索/翻页、clientDetail range），未纳入串行化队列，可按需用请求序号
3. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId）
4. [P3] 仪表盘 img src 可随文案统一限制为 https（低风险）

## 已完成轮次索引
- 第 44 轮（e21343b）：taskService 初始化失败清理+插件配置窗错误页；仪表盘 safeHttpUrl/切项目刷新/串行化防竞态；dashboard 部署 617a3df3
- 第 43 轮（eb5831a）：notice 双写一致性+license 统一兜底+asset.size 强校验+app.quit 落盘+结算超时+mermaid tmp 清理；worker 部署 0f6f3ba7
- 第 42 轮（564e54f）：可研删除/移除双重确认+保存禁用态+文案统一；时长抽共享；worker DELETE RETURNING 对账+sync mustExist
- 第 41 轮（de43f0d + 3f7d330）：AI 队列暂停拒绝统一落 paused 终态；HANDOFF 交接文档；notice 布尔归一（35b1a63）
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口；worker 部署 a21485de
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）
