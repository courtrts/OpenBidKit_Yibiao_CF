# 迭代进度文件

## 当前轮次
第 43 轮待启动（第 42 轮已完成，远端 main = 564e54f，worker 部署 03d7da1d）

## 第 42 轮已完成（2026-09-12）
新增用户功能：
- 可研目录「删除」增加确认弹窗（OutlinePage，AppDialog，含子节点数提示），对齐 technical-plan 交互
- 可研「移除资料」增加确认弹窗（FeasibilityReportHome，明确提示会清空分析/参数/目录/正文）+ IPC 失败补 try/catch+toast（原先静默吞错）

一致性/修复：
- 可研 ContentPage 暂停/失败文案与 technical-plan 统一（三处），失败兜底改「正文生成任务失败」
- ContentPage 章节保存按钮加 savingChapter 禁用态 +「保存中...」，防重复提交
- 新建 client/src/shared/utils/duration.ts 统一时长格式（<1h 为 mm:ss，≥1h 为 h:mm:ss），替换 OutlinePage/ParametersPage/OutlineEditPage/BackgroundTaskTray 四处重复实现（PiAgentMonitorWindow 为刻意的 Xm YYs 开发者格式，保留）
- SettingsPage 保存按钮/状态标签标注当前板块名（保存「文本模型」等），tooltip 说明各板块需分别保存
- worker agentErrorStore deleteRows：配额回滚改为 DELETE...RETURNING 实际命中行对账，防管理端删除与 cron 清理并发双倍扣减 used_bytes
- worker pluginStore syncAllPlugins：mustExist 检查（锁内复查行存在），防快照生成后插件被并发删除又被 sync 连包复活；结果新增 skippedCount

复核：tsc 0 错、23/23 tests、vite build 成功；worker 部署 03d7da1d，线上 health/notice/plugins 200，admin 仪表盘 200。
降级/跳过：无。

## 待办项（审计排队，按序消化）
1. [P2] pluginStore 发布路径 asset.size 缺失时完整性校验静默失效（resolveGitHubPluginOnce 要求 size>0，staging/发布复核 expectedSize<=0 判失败）
2. [P2] notice 管理端删除：KV 删除基于过期 current 判定且先于 D1，存在 D1/KV 分叉窗口（改先删 D1 再复核删 KV）
3. [P2] notice 保存补偿：readStoredNotice 在 saveStoredNotice 之后才读，回写的是新值非旧值（需前置快照旧行）
4. [P2] license 双源默认值：offline 通道 KV 故障时回退请求体布尔值，activate 通道直接 500（抽 resolveLicenseConfigOrDefaults 统一）
5. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId）
6. [P3] 可研 OutlinePage/ParametersPage 的 formatDuration 已统一为 h:mm:ss，可回归确认长任务显示

## 已完成轮次索引
- 第 42 轮（564e54f）：可研删除/移除双重确认+保存禁用态+文案统一；时长抽共享；worker DELETE RETURNING 对账+sync mustExist
- 第 41 轮（de43f0d + 3f7d330）：AI 队列暂停拒绝统一落 paused 终态；HANDOFF 交接文档；notice 布尔归一（35b1a63）
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口；worker 部署 a21485de
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）
