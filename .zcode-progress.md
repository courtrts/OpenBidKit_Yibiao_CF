# 迭代进度文件

## 当前轮次
第 48 轮待启动（第 47 轮已完成，远端 main = 96b581e，dashboard 部署 64c6f689）

## 第 47 轮已完成（2026-09-12）
新增用户功能：
- 正文编辑页（technical-plan）水合期显示「正在读取目录...」加载态，不再把加载中的空树伪装成「暂无目录」（ContentEditPage 新增 outlineLoading prop，复用 Home 的 hydrated 信号）

一致性/修复（导出/模板审计 3 条 + 仪表盘排队 P3）：
- [根因] 模板 store 读取层统一归一化：configStore 导出 normalizeExportFormat，templateStore.templateFromRow 解析 DB JSON 后统一补全缺块——list/get/create/update 全部消费点一处覆盖
- 投标导出链路两个漏点：导出执行（runExportWord）与模板预览（buildExportFormatCssVars/TemplatePreview）改为消费 withExportFormatDefaults 归一化后的 config，缺块历史模板不再白屏/版式错误；与可研链路口径一致
- 仪表盘插件图标/资源图片 URL 与仓库链接同口径仅放行 http(s)（plugins.js/resources.js）

复核：tsc 0 错、23/23 tests、configStore/templateStore 加载 OK、normalizeExportFormat 行为抽查（保留 template_name、补全缺失块）通过；dashboard 部署 64c6f689，admin 域 200。
降级/跳过：审计问题 4（config.load 直读点包归一化，主进程已保证安全）标记为防漂移建议项，暂不执行。

## 待办项（审计排队，按序消化）
1. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId）
2. [建议] config.load 直读点（TechnicalPlanHome:631、OutlineEditPage:449、ContentEditPage:660）包一层 renderer 归一化，防主进程实现漂移

## 已完成轮次索引
- 第 47 轮（96b581e）：模板 store 归一化根因修复+投标导出/预览归一化+仪表盘图片 URL 限 https+目录加载态；dashboard 部署 64c6f689
- 第 46 轮（f06287f）：查重页水合占位+失败不静默重跑+卸载竞态守卫
- 第 45 轮（cac9d5b）：仪表盘过期横幅+局部防竞态+知识库错误空态+复制失败提示；dashboard 部署 4953c997
- 第 44 轮（e21343b）：taskService 初始化失败清理+插件配置窗错误页；仪表盘 safeHttpUrl/切项目刷新/串行化防竞态；dashboard 部署 617a3df3
- 第 43 轮（eb5831a）：notice 双写一致性+license 统一兜底+asset.size 强校验+app.quit 落盘+结算超时+mermaid tmp 清理；worker 部署 0f6f3ba7
- 第 42 轮（564e54f）：可研删除/移除双重确认+保存禁用态+文案统一；时长抽共享；worker DELETE RETURNING 对账+sync mustExist
- 第 41 轮（de43f0d + 3f7d330）：AI 队列暂停拒绝统一落 paused 终态；HANDOFF 交接文档；notice 布尔归一（35b1a63）
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口；worker 部署 a21485de
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）
