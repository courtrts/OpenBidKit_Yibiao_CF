# 迭代进度文件

## 当前轮次
第 46 轮待启动（第 45 轮已完成，远端 main = cac9d5b，dashboard 部署 4953c997）

## 第 45 轮已完成（2026-09-12）
新增用户功能：
- 仪表盘刷新失败时面板顶部显示「内容可能已过期」横幅 + 数据降透明度 + 作废 60s 缓存（main.js markPanelStale + styles.css .is-stale），管理员不再把陈旧数据当最新读数
- 知识库读取失败显示「知识库读取失败」错误空态 + 重试按钮（KnowledgeBasePage loadError state），不再伪装成「还没有文件夹」误导用户以为资料被清空

一致性/修复：
- 仪表盘模型缓存页（防抖搜索/翻页）与客户端详情（范围切换）加请求序号守卫，慢响应不再覆盖新筛选结果
- 可研资料分析「复制」失败（剪贴板权限/焦点丢失）给出错误提示，不再静默无反应 + unhandled rejection

复核：tsc 0 错、23/23 tests；dashboard 部署 4953c997，admin 域 200。
降级/跳过：无。

## 待办项（审计排队，按序消化）
1. [P2] 查重页水合期间无 loading 占位（空态闪屏），且上次失败的分析在打开页面时会自动无确认重跑全量查重（DuplicateCheckPage:685-706/813-822，加 hydrated state 并在水合完成前不触发自动启动）
2. [P3] ContentEditPage 无加载态，空树伪装成「无目录」（需父组件传 loading prop）
3. [P3] useEffect 卸载竞态 3 处（KnowledgeBasePage:353/425-443、ContentEditPage:653-663、DuplicateCheckPage:693-695 catch 未查 canceled）
4. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId）

## 已完成轮次索引
- 第 45 轮（cac9d5b）：仪表盘过期横幅+局部防竞态+知识库错误空态+复制失败提示；dashboard 部署 4953c997
- 第 44 轮（e21343b）：taskService 初始化失败清理+插件配置窗错误页；仪表盘 safeHttpUrl/切项目刷新/串行化防竞态；dashboard 部署 617a3df3
- 第 43 轮（eb5831a）：notice 双写一致性+license 统一兜底+asset.size 强校验+app.quit 落盘+结算超时+mermaid tmp 清理；worker 部署 0f6f3ba7
- 第 42 轮（564e54f）：可研删除/移除双重确认+保存禁用态+文案统一；时长抽共享；worker DELETE RETURNING 对账+sync mustExist
- 第 41 轮（de43f0d + 3f7d330）：AI 队列暂停拒绝统一落 paused 终态；HANDOFF 交接文档；notice 布尔归一（35b1a63）
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口；worker 部署 a21485de
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）
