# 迭代进度文件

## 当前轮次
第 25 轮待启动（第 24 轮已完成，远端 main = ea9d1b2）

## 待办项
1. [P2] 可研章节/目录保存失败静默丢修订——已回归确认（第 17/24 轮修复生效）
2. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId，1-2h，可延后）
3. [P3] pluginStore 发布失败时 asset.size 缺失则大小校验静默失效（低风险）

## 第 24 轮已完成
- SourcesPage 读取三态：0 字解析结果给出"解析为空"空态、读取失败给出错误空态，不再伪装加载中
- notice 更新分支 KV 失败回写旧值，消除 D1/KV 双源静默分叉
- 复核：build/23 tests 全绿；worker 部署 6ccb1d23 线上 notice/health 200
2. [P3] 可研章节状态按标题匹配同名章节会同时标红（需主进程 stats 带 activeNodeId，1-2h，可延后）
3. [P3] pluginStore 发布失败时 asset.size 缺失则大小校验静默失效（低风险）
4. [P2] 可研章节/目录保存失败静默丢修订——FeasibilityReportHome 三个回调已补 try/catch+toast（第 17 轮 336fad6 完成，待回归验证）

## 已完成轮次索引
- 第 23 轮（c5fcda1）：自然化审校任务接入统一暂停入口，托盘与页面暂停承诺一致；远端 worker 部署 a21485de（/track 400、license 布尔归一、model-usage tie-break、禁用公告不计配额）
- 第 22 轮（998c63e）：导出健壮性（深度限制/单节降级/码点截断）
- 第 21 轮（878df55）：工具条 tooltip + 可研大纲保存 catch
- 第 20 轮（73f9f3a + 5373b46）：暂停/取消竞态修复 + 可研审校阶段文案
- 第 19 轮（49c8170）：目录相似度卡片前置 + license 布尔归一 + model-usage tie-break + 禁用公告写配额
- 第 18 轮（084f79d + 17f7727）：一键复制 Markdown 报告 + 导出图片超时/体积上限

## 跳过项
- StartupAdvertisementDialog 死代码删除（多轮审计标记负收益，保持不挂载即可）

## 复核不通过项
- （无）

## 审计排队项（恢复工作时按序消化）
- [P3] 暂停/取消竞态（requestPause abort 抛错）—— 已在第 20 轮修复，勾销
- [P2] license 配置布尔归一 —— 已在第 19 轮修复，勾销
- [P3] model-usage tie-break —— 已在第 19 轮修复，勾销
- [P3] 禁用公告写配额 —— 已在第 19 轮修复，勾销
- [P2] notice 双写更新分支失败回滚（小时级）
- [P2] 模板 config 归一化推广至 ContentPage 的 config.load（第 17 轮已完成 MyTemplates/可研两处，剩余一处低优先）
